[project]
name = "ai_projects"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "cmake>=3.28.4",
    "conan>=2.2.1",
    "ninja>=1.13.0"
]

[project.optional-dependencies]
dev = [
    "clang-tidy>=18.1.1",
    "pre-commit>=3.4.0",
    "pylint>=3.1.0"
]
